apiVersion: apps/v1
kind: Deployment
metadata:
  name: f1-dashboard
  namespace: user-mascret
spec:
  replicas: 1
  selector:
    matchLabels:
      app: f1-dashboard
  template:
    metadata:
      labels:
        app: f1-dashboard
    spec:
      containers:
        - name: f1-dashboard
          image: ghcr.io/nujabest/curly-octo-adventure:latest
          ports:
            - containerPort: 8050
          env:
            - name: FF1_CACHE_DIR
              value: /app/cache
          resources:
            requests:
              memory: "4Gi"
              cpu: "1000m"
            limits:
              memory: "12Gi"
              cpu: "4000m"
          livenessProbe:
            httpGet:
              path: /health
              port: 8050
            initialDelaySeconds: 90
            periodSeconds: 15
            failureThreshold: 5
            timeoutSeconds: 10
          readinessProbe:
            httpGet:
              path: /health
              port: 8050
            initialDelaySeconds: 60
            periodSeconds: 10
            failureThreshold: 5
            timeoutSeconds: 10
